
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stackgres-ui
  namespace: stackgres
  annotations:
    cert-manager.io/cluster-issuer: selfsigned
    nginx.ingress.kubernetes.io/auth-url: https://auth.sigmacloud.app/oauth2/auth
    nginx.ingress.kubernetes.io/auth-signin: https://auth.sigmacloud.app/oauth2/start?rd=$scheme://$host$request_uri
    nginx.ingress.kubernetes.io/auth-response-headers: Authorization
    nginx.ingress.kubernetes.io/auth-snippet: |
      auth_request_set $sso_upstream_1 $upstream_cookie_sso_1;

      access_by_lua_block {
        if ngx.var.sso_upstream_1 ~= "" then
          ngx.header["Set-Cookie"] = "sso_1=" .. ngx.var.sso_upstream_1 .. ngx.var.auth_cookie:match("(; .*)")
        end
      }
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - stackgres.sigmacloud.app
      secretName: stackgres.sigmacloud.app-cert
  rules:
    - host: stackgres.sigmacloud.app
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: stackgres-restapi
                port:
                  number: 80
