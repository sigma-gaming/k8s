apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: pgbouncer
spec:
  folderRef: cloudnative-pg
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  configMapRef:
    name: pgbouncer-dashboard
    key: dashboard.json
