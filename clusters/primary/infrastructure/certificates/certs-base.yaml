apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ingress-base
  namespace: default
spec:
  secretName: ingress-base-tls
  dnsNames:
    - "sigma-k8s.top"
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  subject:
    organizations:
      - "Sigma"