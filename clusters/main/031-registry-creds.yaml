apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: registry-creds-es
  namespace: flux-system
spec:
  interval: 1h
  path: ./cluster-components/main/registry-creds-es
  prune: true
  dependsOn:
    - name: external-secrets-operator
  sourceRef:
    kind: GitRepository
    name: flux-system
