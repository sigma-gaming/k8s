apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cloudflare-tls-mirror
  namespace: flux-system
spec:
  interval: 1h
  path: ./cluster-components/main/cloudflare-tls-mirror
  prune: true
  dependsOn:
    - name: mirrors
  sourceRef:
    kind: GitRepository
    name: flux-system
