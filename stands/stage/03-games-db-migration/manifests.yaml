apiVersion: batch/v1
kind: Job
metadata:
  name: games-db-migration- # version is appended in kustomization.yaml
  namespace: games-stage
spec:
  template:
    spec:
      nodeSelector:
        role: application
        environment: stage
      imagePullSecrets:
        - name: google-registry-creds
      containers:
        - name: games-db-migration
          image: europe-west1-docker.pkg.dev/sigma-games/docker/migrations/games-db-migration:dev-1735944652296 # {"$imagepolicy": "games-stage:games-db-migration"}
          env:
            - name: NODE_ENV
              value: "production"
            - name: GAMES_DB_HOST
              valueFrom:
                secretKeyRef:
                  name: games-db-app
                  key: host
            - name: GAMES_DB_USER
              valueFrom:
                secretKeyRef:
                  name: games-db-app
                  key: user
            - name: GAMES_DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: games-db-app
                  key: password
            - name: GAMES_DB_DATABASE
              valueFrom:
                secretKeyRef:
                  name: games-db-app
                  key: dbname
      restartPolicy: Never
