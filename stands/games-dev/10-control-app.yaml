apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-app
  namespace: games-dev
  labels:
    app: control-app
spec:
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: control-app
  template:
    metadata:
      namespace: games-dev
      labels:
        app: control-app
    spec:
      imagePullSecrets:
        - name: google-registry-creds
      containers:
        - name: control-app
          image: europe-west4-docker.pkg.dev/sigma-games/docker/apps/control-app:dev-1719944998191 # {"$imagepolicy": "games-dev:control-app"}
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
            requests:
              cpu: "250m"
              memory: "256Mi"
          env:
            - name: NODE_ENV
              value: "production"
            - name: PUBLIC_DOMAIN
              value: "sigmacloud.app"
            - name: PUBLIC_GAMES_APP_URL
              value: "https://sigmacloud.app"
            - name: PUBLIC_GAMES_API_URL
              value: "https://api.sigmacloud.app"
            - name: PUBLIC_GAMES_WS_URL
              value: "https://ws.sigmacloud.app"
            - name: PUBLIC_CONTROL_APP_URL
              value: "https://control.sigmacloud.app"
            - name: PUBLIC_CONTROL_APP_VERSION
              value: "dev-1719944998191" # {"$imagepolicy": "games-dev:control-app:tag"}
            - name: PUBLIC_CONTROL_API_URL
              value: "https://control-api.sigmacloud.app"
            - name: PUBLIC_TELEGRAM_BOT_ID
              value: "6551119896"
            - name: PUBLIC_VK_APP_ID
              value: "51812041"
---
apiVersion: v1
kind: Service
metadata:
  name: control-app
  namespace: games-dev
spec:
  selector:
    app: control-app
  ports:
    - protocol: TCP
      port: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: control-app
  namespace: games-dev
  annotations:
    cert-manager.io/cluster-issuer: selfsigned
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - control.sigmacloud.app
      secretName: control.sigmacloud.app-cert
  rules:
    - host: control.sigmacloud.app
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: control-app
                port:
                  number: 80
