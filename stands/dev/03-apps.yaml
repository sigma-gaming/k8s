apiVersion: apps/v1
kind: Deployment
metadata:
  name: games-app
  namespace: dev
  labels:
    app: games-app
spec:
  selector:
    matchLabels:
      app: games-app
  template:
    metadata:
      namespace: dev
      labels:
        app: games-app
    spec:
      imagePullSecrets:
        - name: google-registry-creds
      containers:
        - name: games-app
          image: europe-west4-docker.pkg.dev/sigma-games/docker/apps/games-app:dev-1719682086823 # {"$imagepolicy": "dev:games-app"}
          ports:
            - containerPort: 80
          resources:
            limits:
              cpu: "500m"
              memory: "512Mi"
          env:
            - name: PUBLIC_DOMAIN
              value: "sigmacloud.app"
            - name: PUBLIC_GAMES_APP_URL
              value: "https://sigmacloud.app"
            - name: PUBLIC_GAMES_API_URL
              value: "https://api.sigmacloud.app"
            - name: PUBLIC_TELEGRAM_BOT_ID
              value: "6551119896"
            - name: PUBLIC_VK_APP_ID
              value: "51812041"
---
apiVersion: v1
kind: Service
metadata:
  name: games-app
  namespace: dev
spec:
  selector:
    app: games-app
  ports:
    - protocol: TCP
      port: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: games-app
  namespace: dev
  annotations:
    cert-manager.io/cluster-issuer: selfsigned
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - sigmacloud.app
      secretName: sigmacloud.app-cert
  rules:
    - host: sigmacloud.app
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: games-app
                port:
                  number: 80
