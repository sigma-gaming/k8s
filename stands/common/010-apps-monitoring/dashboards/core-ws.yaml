apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDashboard
metadata:
  name: core-ws
spec:
  folderRef: applications
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  configMapRef:
    name: core-ws-dashboard
    key: dashboard.json
