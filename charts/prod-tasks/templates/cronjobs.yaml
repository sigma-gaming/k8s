{{- range $i, $job := .Values.jobs }}
apiVersion: batch/v1
kind: CronJob
metadata:
  namespace: {{ $.Release.Namespace }}
  name: {{ $job.name | kebabcase }}
spec:
  schedule: "{{ $job.schedule }}"
  timeZone: "Europe/Moscow"
  concurrencyPolicy: Forbid
  jobTemplate:
    spec:
      template:
        spec:
          nodeSelector:
            role: application
            environment: stage
          imagePullSecrets:
            - name: google-registry-creds
          {{- if $.Values.migration.enabled }}
          initContainers:
            - name: wait-for-games-db-migration
              image: ghcr.io/groundnuty/k8s-wait-for:v1.6
              args:
                - "job"
                - "games-db-migration-$(GAMES_DB_MIGRATION_TAG)"
              env:
                - name: GAMES_DB_MIGRATION_TAG
                  value: {{ $.Values.migration.tag }}
          {{- end }}
          containers:
            - name: {{ $job.name | kebabcase }}
              image: "{{ $.Values.image.repository }}:{{ $.Values.image.tag }}"
              resources:
                {{- toYaml $.Values.resources | nindent 16 }}
              env:
                {{- toYaml $.Values.env | nindent 16 }}
                - name: "{{ $.Values.jobEnvName }}"
                  value: "{{ $job.name }}"
---
{{- end }}